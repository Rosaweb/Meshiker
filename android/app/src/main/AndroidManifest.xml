<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Partage de position, mode Auto (spec-partage-position-live-tracking.md
         §6) : alarme exacte pour les check-ins programmés. SCHEDULE_EXACT_ALARM
         (pas USE_EXACT_ALARM, réservé aux apps horloge/alarme par Play Console)
         doit être explicitement demandée à l'utilisateur dès Android 14, voir
         LocationAutoAlarmService. RECEIVE_BOOT_COMPLETED permet à
         android_alarm_manager_plus de replanifier automatiquement les alarmes
         après un redémarrage (utile pour un partage Auto sur plusieurs jours). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permissions pour le stockage (GPX) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Permissions pour le podomètre -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Permission pour le scan de QR code (réception d'un partage) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Photos géolocalisées : lecture de la pellicule pour l'observateur de
         nouvelles images (photo_manager) pendant une session multi-photos.
         Android 13+ ; sous 13 c'est READ_EXTERNAL_STORAGE (déjà déclarée). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Permission pour le micro de l'assistant IA conversationnel -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permissions pour la localisation -->
    <uses-feature android:name="android.hardware.location.gps" />

    <!-- Requis pour le partage GPX via WiFi local (serveur HTTP en clair vers une IP
         locale dynamique) : Android bloque le trafic HTTP non chiffré par défaut depuis
         l'API 28. Une config réseau scopée par domaine n'est pas praticable ici car l'IP
         locale change à chaque réseau WiFi. -->
    <application
        android:label="Meshiker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Service requis pour geolocator en mode foreground -->
        <service
            android:name="com.baseflow.geolocator.GeolocatorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Partage de position, mode Auto : android_alarm_manager_plus
             (entrées reprises telles quelles de son AndroidManifest.xml
             d'exemple, package 5.1.1). RebootBroadcastReceiver activé
             (contrairement au défaut du plugin) pour qu'un partage Auto sur
             plusieurs jours (72h par défaut, voir plan d'implémentation)
             survive à un redémarrage du téléphone. -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Requis à partir d'Android 11 pour que url_launcher puisse ouvrir
             le composeur téléphonique depuis la fiche POI OSM (tel:). -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <!-- Requis par flutter_tts pour la synthèse vocale des annonces de waypoints -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <!-- Requis (Android 11+) pour que resolveActivity() trouve l'appli
             caméra système lancée par le bouton photo du bandeau (spec-photos). -->
        <intent>
            <action android:name="android.media.action.STILL_IMAGE_CAMERA"/>
        </intent>
    </queries>
</manifest>
